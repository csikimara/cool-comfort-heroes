# Frontend environment variables (safe to commit — no secrets).
# Real Supabase values are provided by Lovable Cloud at build time.
VITE_SUPABASE_PROJECT_ID="your-supabase-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-supabase-publishable-anon-key"
VITE_SUPABASE_URL="https://your-supabase-project.supabase.co"

# Cloudflare Turnstile public site key (safe to expose in the browser).
# Get it from https://dash.cloudflare.com/?to=/:account/turnstile
# In local development use Cloudflare's documented test site key explicitly.
VITE_TURNSTILE_SITE_KEY=""

# ---------------------------------------------------------------------------
# Northwind Flow API layer (src/lib/api). Leave the base URL empty to keep the
# integration dormant — the current contact flow is unaffected either way.
# See docs/northwind-flow-api.md
# ---------------------------------------------------------------------------
VITE_FLOW_API_BASE_URL=""
VITE_FLOW_API_VERSION="/api/v1"
VITE_FLOW_API_CLIENT_KEY=""
VITE_FLOW_API_TIMEOUT_MS="15000"
VITE_FLOW_API_RETRIES="2"
VITE_FLOW_API_RETRY_DELAY_MS="400"
# Optional per-endpoint overrides, e.g.:
# VITE_FLOW_EP_LEADS_CREATE="/leads"
# VITE_FLOW_EP_CALENDAR_AVAILABILITY="/calendar/availability"

# ---------------------------------------------------------------------------
# Server-only secrets — DO NOT put real values in this file or in .env.
# Configure them in Lovable Cloud → Backend → Secrets:
#   TURNSTILE_SECRET_KEY   Cloudflare Turnstile secret key (server verify)
#   IP_HASH_SALT           At least 32 random bytes/characters; never reuse an API key
#   RESEND_API_KEY         Resend transactional email API key
#   ALLOW_LOCALHOST_CORS    Keep unset/false in production; local form testing only
#   ALLOW_LOVABLE_PREVIEW  Keep unset/false in production; temporary test-form opt-in only
# ---------------------------------------------------------------------------
